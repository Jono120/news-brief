# Proxy target for the Brief APAC API (must match brief review TLS mode)
VITE_API_PROXY=http://127.0.0.1:8787

# If you run `brief review` with HTTPS (default), use instead:
# VITE_API_PROXY=https://127.0.0.1:8787
